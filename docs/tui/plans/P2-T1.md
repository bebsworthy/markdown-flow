# P2-T1 — Ink project skeleton, build config, binary entrypoint

**Source spec.** `/Users/boyd/wip/markdown-flow/docs/tui/plan.md` lines 216–234.
**References.** features.md §6.1 (Stack), §6.5 (Packaging). Global rules at plan.md lines 48–64.
**Status.** Plan — implementation pending.

---

## 0. Starting-state inventory

- `/Users/boyd/wip/markdown-flow/package.json` already delegates `build`/`test`/`lint` via `--workspaces --if-present` (P0-T1).
- No `tsconfig.base.json` at root — **CREATE** in §2.
- `packages/markflow/tsconfig.json` is strict TS, ES2022, moduleResolution bundler, with `declaration`/`sourceMap`.
- `packages/markflow-tui/package.json` is a minimal stub (name, private, bin → dist/cli.js, `markflow: "*"`).
- `packages/markflow-tui/dist/cli.js` is a placeholder tracked via `.gitignore` negation at `!packages/markflow-tui/dist/cli.js` — **REMOVE** the negation, **GIT RM** the stub.
- No `src/`, no tsconfig, no tsup config, no tests yet in the TUI package.
- Installed (hoisted): `tsup 8.5.1`, `typescript 5.9.3`, `vitest 2.1.9`. No ink/react/@inkjs/ui yet.

---

## 1. Files to create / modify

| Path | Action |
|---|---|
| `/Users/boyd/wip/markdown-flow/tsconfig.base.json` | CREATE (§2) |
| `/Users/boyd/wip/markdown-flow/packages/markflow-tui/tsconfig.json` | CREATE (§3) |
| `/Users/boyd/wip/markdown-flow/packages/markflow-tui/package.json` | REPLACE (§4) |
| `/Users/boyd/wip/markdown-flow/packages/markflow-tui/tsup.config.ts` | CREATE (§5) |
| `/Users/boyd/wip/markdown-flow/packages/markflow-tui/src/cli.tsx` | CREATE (§6) |
| `/Users/boyd/wip/markdown-flow/packages/markflow-tui/src/app.tsx` | CREATE (§7) |
| `/Users/boyd/wip/markdown-flow/.gitignore` | MODIFY — remove `!packages/markflow-tui/dist/cli.js` negation (§8) |
| `/Users/boyd/wip/markdown-flow/packages/markflow-tui/dist/cli.js` | `git rm` (stub); regenerated by build, then gitignored (§8) |
| `/Users/boyd/wip/markdown-flow/package.json` (root) | unchanged — already fan-outs |
| `/Users/boyd/wip/markdown-flow/packages/markflow/tsconfig.json` | unchanged (do NOT fold into base this task) |

---

## 2. `tsconfig.base.json` (new, repo root)

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "jsx": "react-jsx"
  }
}
```

**Do NOT refactor `packages/markflow/tsconfig.json` to extend this base** in P2-T1. Minimal-change principle: engine tests are green, don't risk regressions. Flag as opportunistic follow-up in §11.

---

## 3. `packages/markflow-tui/tsconfig.json` (new)

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "declaration": false,
    "sourceMap": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist", "test", "vhs"]
}
```

- `declaration: false` — binary, not a library.
- `exclude` covers future `test/` (P2-T2) and `vhs/` (P2-T3).

---

## 4. `packages/markflow-tui/package.json` (replace stub)

```json
{
  "name": "markflow-tui",
  "version": "0.0.0",
  "private": true,
  "description": "Terminal UI for the markflow workflow engine.",
  "type": "module",
  "bin": {
    "markflow-tui": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx watch src/cli.tsx",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "markflow": "*",
    "ink": "^5.0.1",
    "@inkjs/ui": "^2.0.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.12",
    "ink-testing-library": "^4.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
```

- `markflow: "*"` — stays (npm rejected `workspace:*` in P0-T1).
- Ink 5 peer-deps React 18; pin accordingly.
- `@inkjs/ui` declared even though scaffold doesn't import it (future Phase-4/5 uses it).
- If npm flags `@inkjs/ui@^2.0.0` as unavailable, fall back to `^1.x` and flag in follow-up. Do NOT block on this.

---

## 5. `packages/markflow-tui/tsup.config.ts` (new)

```ts
import { defineConfig } from "tsup";

export default defineConfig({
  entry: { cli: "src/cli.tsx" },
  outDir: "dist",
  format: ["esm"],
  target: "es2022",
  platform: "node",
  sourcemap: true,
  clean: true,
  dts: false,
  shims: false,
  banner: {
    js: "#!/usr/bin/env node",
  },
  external: ["react", "react/jsx-runtime", "ink", "@inkjs/ui", "markflow"],
});
```

- ESM only; shebang injected via `banner.js`.
- Externalise React/Ink/markflow (avoids bundling Ink's Node APIs; node_modules is always present at runtime via npm).
- `dts: false` — binary, not a library.

---

## 6. `packages/markflow-tui/src/cli.tsx` (new)

```tsx
import { render } from "ink";
import React from "react";
import { App } from "./app.js";

const { unmount, waitUntilExit } = render(<App onQuit={() => unmount()} />);

waitUntilExit().then(
  () => process.exit(0),
  (err) => {
    console.error(err);
    process.exit(1);
  },
);
```

- Shebang comes from tsup banner, NOT this file.
- Import path `./app.js` (moduleResolution bundler accepts `.js` for `.tsx`).
- SIGINT handled by Ink's built-in.
- Mount boundary is trivial; App carries the `q` handler.

---

## 7. `packages/markflow-tui/src/app.tsx` (new)

```tsx
import { Text, useInput } from "ink";
import React from "react";

export interface AppProps {
  onQuit: () => void;
}

export function App({ onQuit }: AppProps): React.ReactElement {
  useInput((input) => {
    if (input === "q") {
      onQuit();
    }
  });

  return <Text>markflow-tui · scaffold</Text>;
}
```

- Named export `App` + `AppProps` → P2-T2 test harness can mount directly.
- Text content verbatim per spec: `markflow-tui · scaffold` (middot U+00B7).
- `onQuit` prop rather than `useApp().exit()` → testable.

---

## 8. `.gitignore` update + stub removal

1. Remove the line `!packages/markflow-tui/dist/cli.js` from `.gitignore`.
2. `git rm packages/markflow-tui/dist/cli.js`.
3. After first `npm run build`, `dist/cli.js` is regenerated untracked (matches engine convention).

**Order:** `npm install` (recreates `.bin/markflow-tui` symlink) → edit `.gitignore` → `git rm` stub → `npm run build` (rebuilds into dist).

---

## 9. Root `package.json` scripts — NO change

Already fan-outs via `--workspaces --if-present` (P0-T1). Once TUI package has its scripts, they're automatically picked up.

---

## 10. Validation

From repo root:

```bash
# Install new deps
npm install

# Acceptance commands
npm run lint -w packages/markflow-tui    # tsc --noEmit, exit 0
npm run build -w packages/markflow-tui   # produces dist/cli.js with shebang

# Runtime smoke (MANUAL — Ink's useInput requires a TTY)
head -1 packages/markflow-tui/dist/cli.js   # → #!/usr/bin/env node
node packages/markflow-tui/dist/cli.js      # renders "markflow-tui · scaffold"; press q → exit 0

# Engine regression gates
npm run lint -w packages/markflow
npm test -w packages/markflow -- --run
npm run build -w packages/markflow

# Root-level fan-out
npm run lint
npm run build
npm test -- --run
```

**Interactive smoke check is MANUAL** — Ink's `useInput` needs a real TTY. `echo q | node dist/cli.js` won't work; automated interactive coverage lands with `ink-testing-library` in P2-T2. For P2-T1 acceptance, it's sufficient to confirm:

- Shebang on first line of `dist/cli.js`.
- Lint + build exit 0.
- Engine tests still green.
- No extra runtime deps beyond Ink + @inkjs/ui + React + markflow.

---

## 11. Risks / unknowns

1. **Ink 5 ESM strictness** — mitigated by `"type": "module"` + ESM tsup output.
2. **React 18 / Ink 5 / @inkjs/ui peer-deps** — pinned to compatible ranges. Check `npm ls` for UNMET PEER WARN after install.
3. **`@inkjs/ui@^2.0.0` availability** — if only 1.x is published as of 2026-04, downgrade pin and note in follow-up. Not blocking (scaffold does not import it).
4. **`ink-testing-library@^4.0.0` React 18 compat** — confirmed current major supports Ink 5 + React 18.
5. **tsup banner + ESM prologue** — validated behaviour in tsup 8.x; shebang before imports works in Node 18+.
6. **`tsc --noEmit` scope** — `include: ["src"]` keeps it tight; no stray files type-checked.
7. **dangling `.bin` during stub removal** — order: install → rm stub → build.
8. **Folding engine tsconfig into base** — deferred to a future cleanup task.

---

## 12. Execution order (for implementer)

1. Create `tsconfig.base.json` at repo root.
2. Create `packages/markflow-tui/tsconfig.json`.
3. Replace `packages/markflow-tui/package.json`.
4. Create `packages/markflow-tui/tsup.config.ts`.
5. Create `packages/markflow-tui/src/cli.tsx`.
6. Create `packages/markflow-tui/src/app.tsx`.
7. `npm install` at repo root.
8. Edit `.gitignore`: remove the stub negation line.
9. `git rm packages/markflow-tui/dist/cli.js`.
10. `npm run build -w packages/markflow-tui` — regenerates `dist/cli.js`.
11. `npm run lint -w packages/markflow-tui`.
12. Manual smoke test: `node packages/markflow-tui/dist/cli.js`, press `q`, verify exit 0.
13. Engine regression gates.
14. Report status; do NOT commit (orchestrator commits).

---

## 13. Commit message template

```
feat(tui): scaffold Ink project with build config and binary entrypoint

- Implements P2-T1 from docs/tui/plan.md.
- References: features.md §6.1, §6.5.
```
